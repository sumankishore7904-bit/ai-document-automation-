import os
from dotenv import load_dotenv

# Loads the hidden .env file from your iPad
load_dotenv()

# Reads the values
api_key = os.getenv("API_KEY")
ai_model = os.getenv("AI_MODEL")

print("Using model:", ai_model)
